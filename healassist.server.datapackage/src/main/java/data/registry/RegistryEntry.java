package data.registry;

import data.general.IUser;

public class RegistryEntry implements IEntry {
	public IUser GetUser(){
		return this.user;
	}
	public Boolean SetUser(IUser newUser){		
		try{
			if(newUser instanceof )
		}
	}
	
	private IUser user;
}
